<?xml version="1.0" encoding="utf-8"?>
<!--
  Color resources are defined per-theme via Material3 ColorScheme in
  presentation/theme/Color.kt (Spec 003 owns this). Spec 001 leaves this file
  intentionally minimal — adding `<color>` entries here would be a Constitution
  §III violation (colors must come from theme tokens, not raw hex literals).
-->
<resources>
</resources>
