<?xml version="1.0" encoding="utf-8"?><!--
   Auto Backup rules for Android < 12 (API 23–30).
   Spec 005 FR-016 + Q4 clarification: ThirtySixBrowser's Room database is excluded
   from Auto Backup to enforce the on-device-only privacy stance. WAL sidecar files
   (-wal, -shm) are also excluded because they may contain transient transaction
   data that would otherwise produce partial-database backup edge cases.
   See https://developer.android.com/guide/topics/data/autobackup
-->
<full-backup-content>
    <exclude domain="database" path="thirtysix_browser.db"/>
    <exclude domain="database" path="thirtysix_browser.db-wal"/>
    <exclude domain="database" path="thirtysix_browser.db-shm"/>
</full-backup-content>
