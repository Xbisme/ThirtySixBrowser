// Spec 003 — Theme mode enum. Persistence in DataStore comes in Spec 006; UI toggle in Spec 016.

package com.raumanian.thirtysix.browser.presentation.theme

enum class ThemeMode { Light, Dark, System }
